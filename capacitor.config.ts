import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'GraveTransporterApp',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
